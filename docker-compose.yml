services:
  webshell:
    build: .
    ports:
      - "2222:2222"
    environment:
      - PORT=2222
      - WORKSPACE_DIR=/workspace
      - RUST_LOG=info
    volumes:
      - workspace:/workspace
    restart: unless-stopped

volumes:
  workspace:
