services:
  webshell:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - WORKSPACE_DIR=/workspace
      - RUST_LOG=info
    volumes:
      - workspace:/workspace
    restart: unless-stopped

volumes:
  workspace:
